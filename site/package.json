{
  "name": "hardcode-replacer-site",
  "version": "1.0.0",
  "description": "Project website for hardcode-replacer CLI tool",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --port 5174",
    "build": "tsc && vite build",
    "preview": "vite preview --port 5174"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "vite": "^6.2.0"
  }
}
